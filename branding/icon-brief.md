<!--
  SOURCE OF TRUTH for the site icon — the browser favicon AND the home-screen
  app icon (iOS "Add to Home Screen", Android/PWA install, etc).

  Edit the brief below in plain English, then run `npm run icon`. The
  generator (scripts/generate-icon.mjs) hashes this file and skips calling the
  image API when the hash matches branding/icon.meta.json's last recorded
  hash — so editing this brief is what triggers a regeneration; re-running the
  script with nothing changed is a no-op. Use `npm run icon -- --force` to
  regenerate anyway (e.g. to reroll the same brief for a different result).

  Keep the brief a single square, full-bleed emblem: iOS/Android apply their
  own rounding and drop shadow on top of whatever square you give them, and a
  16px favicon has no room for fine detail — bold, high-contrast, and simple
  reads best at every size.
-->

A bold, flat, vector-style monogram badge for "Tonight's Menu," a recipe
flipbook app — NOT a photograph, NOT 3D, NOT photorealistic. A single elegant
serif monogram combining the letters "T" and "M" (like a hand-stamped menu
crest), centered on a full-bleed square, deep brick-red background
(#9c3620). Render the monogram in warm parchment cream (#ece3cd), with a
thin brass-gold (#9a7223) ring inset just inside the square's edge. Generous
padding around the letterforms so the mark still reads clearly as a tiny
16px favicon and as a rounded iOS home-screen icon — no fine detail, no
extra text, no photorealism, no gradients, no drop shadows, no textures.
Confident, warm, a little playful — like an embossed stamp on the cover of
a well-loved menu.
